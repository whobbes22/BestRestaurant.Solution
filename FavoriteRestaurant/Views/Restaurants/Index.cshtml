@{
  Layout = "_Layout";
}

@using FavoriteRestaurant.Models;
@model List<FavoriteRestaurant.Models.Restaurant>;

<h1>Restaurants</h1>

@if (@Model.Count == 0)
{
  <h3>Fool no Restaurants have been added yet! What did you expect?</h3>
} 

<ul>
  @foreach (Restaurant restaurant in Model)
  {
     <li>@Html.ActionLink($"{restaurant.RestaurantName}","Details")</li> // may need to add more
  }

</ul>

<p>@Html.ActionLink("Add new retaurant", "Create")</p>
<p>@Html.ActionLink("Retrun to Home", "Index", "Home")</p>